name: GitHub Env Demo
run-name: 'Example Git Hub Events'

on:
  push:
    branches:
      - main


jobs:
  env_variables_example:
    runs-on: ubuntu-latest
    steps:
      - env:
          name: 'manamohan'
          role: 'TC'
        run: echo 'Name is ${{ env.name }} , Role is ${{ env.role }}'
